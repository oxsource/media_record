package(default_visibility = ["//visibility:public"])

# MultiViewLayoutNode (spec 002): graph_runtime node; single-view composition
# via native_ui flex layout (Container + ExternalImage) + software blit into
# the RGBA frame.

cc_library(
    name = "multi_view_layout",
    srcs = ["multi_view_layout_node.cc"],
    hdrs = ["multi_view_layout_node.h"],
    deps = [
        "@graph_runtime//src/framework/public:runtime",
        "@native_ui//:native_ui",
        "@video_codec//src/framework/public:video_codec",
    ],
    alwayslink = True,
    visibility = ["//visibility:public"],
)
